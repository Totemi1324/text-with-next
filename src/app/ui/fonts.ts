import { Julius_Sans_One, Saira } from "next/font/google";

export const primary_font = Saira({
    weight: ["400", "700"],
    subsets: ["latin"],
});

export const secondary_font = Julius_Sans_One({
    display: "block",
    weight: "400",
    subsets: ["latin"],
});